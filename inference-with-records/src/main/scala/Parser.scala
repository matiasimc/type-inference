/**
  * Created by mmeneses on 15-08-17.
  */


object Parser {
  def parseExpr(sexpr: String): Expr = {
    return null
  }

  def parse(code: String): Expr = {
    return null
  }


  //def executeFun(expr: Expr): Env = {
  //  case Fun(id, parameter, body) => aEnv(id, body, env)
  //}

  //def replace(arg: Expr, id)
}